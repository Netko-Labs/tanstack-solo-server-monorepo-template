import { type {{ pascalCase name }}Config, {{ pascalCase name }}ConfigSchema } from '@temp-repo/{{ name }}-domain'

/**
 * ✧･ﾟ: *✧･ﾟ:* {{ constantCase name }} ENVIRONMENT CONFIG *:･ﾟ✧*:･ﾟ✧
 *
 * Environment configuration for the {{ name }} app (◕‿◕✿)
 */

const {{ camelCase name }}Config: {{ pascalCase name }}Config = {
  app: {
    dev: process.env.NODE_ENV !== 'production',
    baseUrl: process.env.BASE_URL ?? 'http://localhost:3000',
    port: Number(process.env.PORT ?? 3000),
    cors: process.env.CORS?.split(',') ?? [],
    encryptionKey: process.env.ENCRYPTION_KEY ?? '',
  },
  cache: {
    url: process.env.CACHE_URL ?? '',
  },
  db: {
    url: process.env.DATABASE_URL ?? '',
  },
  auth: {
    secret: process.env.AUTH_SECRET,
  },
}

export const {{ camelCase name }}EnvConfig = {{ pascalCase name }}ConfigSchema.parse({{ camelCase name }}Config)

